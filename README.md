# Random Task Generator

**Автор:** Габбазов Алмаз

**Назначение:** генератор случайных задач для итоговой аттестации по курсу «Код будущего».

## Структура проекта

**Описание файлов:**

* `app.py` — главный скрипт программы. Содержит:
    * графический интерфейс на `tkinter`;
    * логику генерации случайных задач;
    * функции добавления новых задач;
    * механизмы фильтрации по категориям;
    * загрузку/сохранение задач и истории.
* `tasks.json` — хранит все доступные задачи в формате JSON. Каждая задача содержит:
    * `"category"` — категория задачи (`спорт`, `работа`, `учёба`, `отдых`);
    * `"task"` — текст задачи.
* `history.json` — история всех сгенерированных задач. Каждая запись содержит:
    * дату и время генерации;
    * категорию задачи;
    * текст задачи.
* `README.md` — текущая документация с инструкциями по запуску и использованию.

## Требования

* Python 3.6 или выше;
* стандартная библиотека `tkinter` (включена в Python);
* стандартная библиотека `json` (включена в Python).

## Шаги запуска программы

1. **Скачайте проект:**
    * клонируйте репозиторий или скачайте файлы в одну папку;
    * убедитесь, что все файлы из раздела «Структура проекта» присутствуют.
2. **Откройте терминал/командную строку** и перейдите в папку с проектом:
    ```bash
    cd путь/к/папке/с/проектом
    ```
3. **Запустите приложение:**
    ```bash
    python app.py
    ```
4. **Дождитесь загрузки интерфейса** — откроется окно программы с тремя основными зонами.

## Интерфейс программы

Окно программы разделено на три функциональные зоны:

1. **Левая часть (Добавление задач):**
    * поле ввода для новой задачи;
    * список категорий (`спорт`, `работа`, `учёба`, `отдых`);
    * кнопка **«Добавить»** для сохранения новой задачи.
2. **Центральная часть (Генерация):**
    * кнопка **«Сгенерировать»** для получения случайной задачи;
    * область отображения результата (выбранная задача с категорией).
3. **Правая часть (История):**
    * список всех ранее сгенерированных задач с датой и временем.

## Примеры работы

### Пример 1. Генерация случайной задачи

1. Запустите программу (`python app.py`).
2. Не выбирая категорию, нажмите кнопку **«Сгенерировать»**.
3. В центральной части окна появится случайная задача из всех доступных, например:
4. Эта задача автоматически добавится в историю справа с отметкой времени.

### Пример 2. Фильтрация по категории

1. В левой части окна выберите категорию из списка, например, **«спорт»**.
2. Нажмите **«Сгенерировать»**.
3. Программа покажет случайную задачу только из категории «спорт», например:
4. Задача появится в истории с указанием категории и времени генерации.

### Пример 3. Добавление своей задачи

1. В поле ввода в левой части окна напишите новую задачу, например, «Пробежать 5 км».
2. Выберите категорию, например, **«спорт»**.
3. Нажмите кнопку **«Добавить»**.
4. Вы увидите сообщение «Задача успешно добавлена».
5. Новая задача теперь доступна для генерации. Проверьте это, нажав **«Сгенерировать»** с выбранной категорией «спорт».

### Пример 4. Просмотр истории

1. Сгенерируйте несколько задач разными способами (с фильтрацией и без).
2. Посмотрите на правую часть окна — там отображается вся история в хронологическом порядке (последние сверху).
3. Закройте программу и запустите её снова — история автоматически загрузится из `history.json`.

## Устранение неполадок

* **«Файл не найден» при запуске:**
 * убедитесь, что вы запускаете `app.py` из правильной папки;
 * проверьте, что файлы `tasks.json` и `history.json` присутствуют (если их нет, программа создаст их автоматически).
* **Не добавляются новые задачи:**
 * проверьте, что заполнены оба поля: текст задачи и выбрана категория;
 * убедитесь, что в тексте задачи нет специальных символов, нарушающих JSON‑формат.
* **История не сохраняется:**
 * проверьте права на запись в папке проекта;
 * убедитесь, что файл `history.json` не открыт в другом приложении.

## Лицензия

Проект распространяется под лицензией MIT. Подробности в файле `LICENSE` (если предоставлен).


## Описание

Программа помогает принимать решения при выборе деятельности — генерирует случайные задачи из списка с возможностью фильтрации по категориям и сохранением истории.

## Функциональность

* **Генерация случайных задач** — выбор случайной задачи из общего списка или отфильтрованного по категории.
* **Добавление пользовательских задач** — возможность добавлять свои задачи с присвоением категории.
* **Фильтрация задач** — отбор задач по категориям («Учёба», «Спорт», «Работа»).
* **История генераций** — отображение всех сгенерированных задач в таблице с датой и временем.
* **Сохранение истории** — автоматическое сохранение в JSON‑файл между запусками.

## Инструкция по запуску

### Требования

* Python 3.6 или выше;
* стандартная библиотека `tkinter` (включена в Python).

### Шаги по запуску

1. Убедитесь, что Python установлен и доступен в командной строке.
2. Скачайте файл `RandomTaskGenerator.py` на ваш компьютер.
3. Откройте командную строку (терминал) и перейдите в папку с файлом:
   ```bash
   cd путь/к/папке/с/файлом

